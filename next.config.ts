import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: 'export',
  basePath: '/react-to-do',  // Nome do repositório
  assetPrefix: '/react-to-do/',  // Nome do repositório
};

export default nextConfig;
